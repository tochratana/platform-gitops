app:
  name: "tochratana-businessweb-4620"
  userId: "159990218"
  projectName: "tochratana-businessweb-4620"
  namespace: "user-159990218"
  framework: "nodejs"
  containerPort: 3000
  servicePort: 80
  domain: "tochratana.com"
  host: "tochratana-businessweb-4620-ws-159990218.tochratana.com" # managed-by-jenkins-host

image:
  repository: "harbor.devith.it.com/deployment-pipeline/159990218/tochratana-businessweb-4620"
  tag: "159990218-107-18527e6491ec" # managed-by-jenkins-image-tag
  pullPolicy: "IfNotPresent"

replicaCount: 1

resources:
  requests:
    cpu: "100m"
    memory: "128Mi"
  limits:
    cpu: "500m"
    memory: "512Mi"

autoscaling:
  enabled: true
  minReplicas: 1
  maxReplicas: 4
  targetCPUUtilizationPercentage: 70

probes:
  readiness:
    enabled: true
    path: "/"
    initialDelaySeconds: 10
    periodSeconds: 10
  liveness:
    enabled: true
    path: "/"
    initialDelaySeconds: 30
    periodSeconds: 15

ingress:
  enabled: true
  className: "nginx"
  annotations:
    cert-manager.io/cluster-issuer: "letsencrypt-prod"
  tls:
    enabled: true
    secretName: "tochratana-businessweb-4620-tls"

imagePullSecrets:
  - name: "registry-secret"

probes:
  mode: "http"
  startup:
    enabled: false
    initialDelaySeconds: 0
    periodSeconds: 5
    failureThreshold: 24
  readiness:
    enabled: true
    path: "/"
    initialDelaySeconds: 10
    periodSeconds: 10
    failureThreshold: 3
  liveness:
    enabled: true
    path: "/"
    initialDelaySeconds: 30
    periodSeconds: 15
    failureThreshold: 3
